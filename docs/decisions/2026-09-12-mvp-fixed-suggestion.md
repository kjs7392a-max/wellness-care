# MVP 시연용 「AI 오늘의 제안」 고정 (2026-09-12)

## 결정
시연 시각이 **평일 16:30~17:30** 이라 홈 「AI 오늘의 제안」과 「지금 바로 시작하기」를
**「퇴근 전 어깨·목 풀기」(가이드 영상 있음) 하나로 고정**한다. 시각·요일·직군에 따라 제안이
달라지던 원래 설계는 **지우지 않고 플래그 뒤에 남긴다.**

- 플래그: `src/features/wellness/suggestion.ts` → `MVP_FIXED_SUGGESTION = true`
- 고정값: 교사 직군 3번째 항목(`ROLES.teacher.items[2]`), PAR-Q 해당자는 저강도판(`low[2]`) — 둘 다 같은 영상.
- 고정 시 `slot = 2`(퇴근 전 문구 세트) · `isWeekend = false`(주말이어도 제목의 "퇴근 전 "을 떼지 않고, 쉬는 날 문구로 바뀌지 않는다).

## 원래 설계 (복원 시 돌아오는 동작)
| 축 | 규칙 | 정의처 |
|---|---|---|
| 시간대 | 11시 전 `slot 0` · 15시 전 `slot 1` · 그 뒤 `slot 2` | `data.ts slotForHour` |
| 직군 | `ROLES[직군].items[slot]` — 교사·행정·급식/보건/조리 각 3개 | `data.ts ROLES` |
| PAR-Q | 해당 항목이 하나라도 있으면 `.low[slot]`(저강도판) | `suggestion.ts` |
| 주말 | 제목에서 "퇴근 전 " 제거 + 제안 문구가 쉬는 날용 | `WellnessApp.tsx compute / renderHome` |
| 제안 문구 | `LEAD_IN[slot] + BODY[직군][slot] + 날씨별 마무리` | `WellnessApp.tsx renderHome daySolution` |

⚠ 제안 **문구**(`daySolution`)의 직군별 본문은 고정하지 않았다 — 직군 선택은 자세 카드 등 다른 화면도 쓰므로
그대로 두고, `slot=2` 로만 묶어 "퇴근 전" 톤이 유지되게 했다.

## 복원 절차
1. `suggestion.ts` 의 `MVP_FIXED_SUGGESTION` 을 `false` 로.
2. `suggestion.test.ts` 의 「플래그가 켜져 있다」 단언을 `false` 로 바꾸거나 삭제.
3. 끝. 나머지 코드는 플래그 off 경로가 원래 설계 그대로다(테스트 「원래 설계(플래그 off)」가 지킨다).

---

## 2026-09-13 — 해제함 (사용자 지시 "해")

`MVP_FIXED_SUGGESTION = false`. 이유 = 고정해 두면 **직군을 골라도 제안 항목이 안 바뀌어**
「어떤 일을 하고 계신가요?」가 실제로 값을 하지 않는다(그날 확인: 직군이 바꾸는 것은 홈 본문
첫 한 문장과 상단 꼬리표뿐이었다).

- `suggestion.test.ts` 의 플래그 단언을 `false` 로 바꿨다. **고정 경로 코드와 그 테스트는 그대로 남겼다** —
  다시 시연용으로 묶어야 하면 플래그 한 줄만 `true` 로.
- ⚠ 해제 후 달라지는 것: 시간대별 제안(11시 전/15시 전/그 뒤) · 직군별 항목 · 주말이면 제목에서
  "퇴근 전 " 이 빠지고 쉬는 날 문구로 바뀜. **평일 16:30~17:30 · 교사 직군에서는 고정 때와 같은 항목**
  (`ROLES.teacher.items[2]` = 퇴근 전 어깨·목 풀기, 가이드 영상)이 나온다 — 그 시연 조건은 영향을 안 받는다.

---

## 2026-09-13 ② — 시간대만 다시 고정 (사용자 지시)

*"ai제안과 제안 스트레칭은 오후 4시 30분부터 오후 5시 30분까지로 설정해주고 행정직도 조리원직도 전부 그 시간대로 설정해서 박제해두고 그 시간에 맞는 제안을 표현해주면 되잖아 스트레칭도 타이머 대신 제안에 맞는 스트레칭 영상을 올려놓으면 되잖아"*

**항목 하나로 묶던 옛 고정(2026-09-12)과 다르다 — 고정하는 것은 「시간대」뿐이고 직군은 안 고정한다.**

- `MVP_FIXED_SUGGESTION`(boolean) → **`DEMO_PINNED_SLOT: number | null = 2`**. `2` = 퇴근 전(16:30~17:30 대).
  `null` 로 두면 실제 시각·요일을 따르는 원래 설계로 돌아간다. 두 갈래 코드가 한 줄로 합쳐졌다.
- 고정 중에는 **주말이어도 평일로 본다** — 「퇴근 전 …」 제목과 「오늘은 쉬는 날이네요」 문구가 부딪히기 때문
  (고정을 풀면 주말 처리가 그대로 돌아온다).
- 결과: 언제 열어도 세 직군이 **각자 다른 퇴근 전 제안**을 받는다.
  교사 「퇴근 전 어깨·목 풀기」 · 행정 「퇴근 전 허리 회복 스트레칭」 · 영양·보건·조리 「퇴근 전 다리 부종 덜어내기」.

### 가이드 영상을 여섯 칸 전부에 붙였다
「지금 바로 시작하기」가 타이머가 아니라 영상으로 열려야 하는데, 영상이 **교사 퇴근 전 하나에만** 붙어 있었다.

| 직군 | 평소 강도(`items[2]`) | 낮은 강도(`low[2]`) |
|---|---|---|
| 교사 · 담임 | 어깨·목 풀기 → `shoulder-release-1m` | 앉은 채로 어깨 내려놓기 → 같은 영상 |
| 행정 직군 | 허리 회복 → `p4-seated-twist`(라이브러리 「퇴근 전 허리 회복」) | 앉은 자리 허리 부담 덜기 → `p5-pelvic-tilt`(**라이브러리와 이름이 글자 그대로 같다**) |
| 영양·보건·조리 | 다리 부종 덜어내기 → `p9-ankle-pump` | 앉아서 다리 쉬게 하기 → 같은 영상 |

🚫 `p8-calf`(종아리)는 **서서** 하는 동작이라 「앉아서」와 어긋나 쓰지 않았다.
🚫 이름과 안 맞는 영상을 채우지 말 것 — 없는 것을 있는 척하는 화면이 타이머보다 나쁘다.
가드 = `suggestion.test.ts` 「고정된 시간대의 항목은 직군·강도와 무관하게 전부 가이드 영상을 갖는다」(붙이기 전 RED 확인).
