# 2026-09-16 웰니스 케어 작업일지

## 1 — 온보딩 「이 앱은 선생님을 평가하지 않습니다」 장의 문장 셋 삭제 (사용자 지시)
- 지운 것 = `PRINCIPLES` 의 「수집은 언제든 중단할 수 있습니다」·「전체 삭제는 즉시, 되돌릴 수 없게 이루어집니다」 + 장 본문의 「시작하기 전에 다섯 가지를 먼저 약속드립니다.」(본문은 「점수나 순위를 매기지 않아요.」만 남음). 원칙 **5 → 3**.
- ★`PRINCIPLES` 는 **설정 시트에서도 그려진다**(`WellnessApp.tsx` 「개인정보 5대 원칙」 상자). 배열만 줄이면 제목이 「5대」인데 세 줄만 보이므로 제목을 **「개인정보 3대 원칙」**으로 함께 고쳤다. README 의 「5대 원칙」도 3대로(그 줄이 09-13 장 합침도 반영 안 된 낡은 문장이라 「온보딩 4단계(3대 원칙 · 수집동의+권한 · 직군 · PAR-Q+ 7문항)」로 바로잡음).
- 🚫 설정 시트의 **행동 항목**(「수집 중단하기」·「전체 데이터 즉시 파기」)은 지시 밖이라 그대로 — 지운 건 약속 문장이지 기능이 아니다.
- 「무엇을 왜 모으는지 언제든 확인할 수 있습니다」는 지시에 없어 남겼다.

### 검증
tsc 0 · **100/100** · build · 옛 문장 grep 잔존 0 · ★실화면(dev 3010): 첫 장 = 제목 아래 「점수나 순위를 매기지 않아요.」 한 줄 + 원칙 상자 **3개** · 온보딩 통과 후 설정 시트 = 「개인정보 **3대** 원칙」 + 같은 3줄 · 「5대」·「언제든 중단」·「전체 삭제는」 페이지 텍스트 0건.
- 푸시 `bc78f0a` · 배포 **`dpl_FtMri5vJ6shPfbiVPndmE4Wg6dBP`**(`readyState: READY` · 별칭 `wellness-care-zeta.vercel.app` 일치) · ★라이브 청크 실물 대조 = 옛 문구 3종·「5대 원칙」 **0건**, 「3대 원칙」·「점수나 순위를 매기지 않아요」 각 1건.
- 배포 **`dpl_8P9CT7tTiSZBL5hdeWKeFAbzoGqB`**(READY · 별칭 `wellness-care-zeta.vercel.app` 일치) · 라이브 청크 「먼저 약속드립니다」 **0건**, 대조군 「안전 확인」·「3대 원칙」 각 1건.

## 3 — 「수집동의서 및 권한허용」 장에서 본문 + 「모으는 것은 이만큼이 전부예요」 목록 삭제 (사용자 지시)
- 지운 것 = `OB[1].body`(「모으는 것은 아래 세 가지가 전부이고 … 더 정확한 결과를 얻을 수 있습니다.」) + 소제목 「모으는 것은 이만큼이 전부예요」 + `COLLECT` 카드 3장(걸음과 활동·함께한 몸풀기·움직인 시간). `COLLECT` 배열은 더 이상 아무도 안 읽어 `data.ts` 에서 함께 제거(import 도).
- 남은 것 = 「동의」 카드 1 + 「권한」 3. ★동의 카드 설명(「걸음·움직인 시간·앱에서 함께한 몸풀기 기록은 암호화되어…」)은 지시 밖이라 그대로 — 그래서 페이지에 「함께한 몸풀기」·「움직인 시간」 글자는 그 문장 안에 남아 있다(목록이 남은 게 아니다).
- README 온보딩 줄 「수집동의+권한」→「동의+권한」.
- 검증: tsc 0 · 100/100 · build · ★실화면: 장이 제목 → 「동의」 → 「권한」 순으로 바로 이어지고 「모으는 것은」·「걸음과 활동」·「더 정확한 결과」 0건.
- 배포 **`dpl_BtQAAijGcahgECrLNHK4S4HxdqvQ`**(READY · 별칭 `wellness-care-zeta.vercel.app` = `1kqg3dbpm` 일치) · 라이브 청크 「모으는 것은」·「걸음과 활동」·「더 정확한 결과」 **0건**, 대조군 「수집·이용 동의 (필수)」·「Health Connect」 각 1건.

## 4 — 안전 확인(옛 PAR-Q+)을 APSS·ACSM 기반 8문항 + 「문항 성격」 판정으로 교체 (사용자 확정)

### 발단
*"par-q+ 가 … 설정이 대충이라 체육학적으로 정확하게 스코어링 할 수 있어?"* → 조사 결과 **PAR-Q+ 는 점수 도구가 아니라 예/아니오 결정 트리**이고, 지금 앱의 「예 개수」 문턱(`PARQ_TIER_AT = {low:1, rest:3}`)은 어느 표준의 규칙도 아니었다(주석에도 「임상 감수 대상」이라 박혀 있었다). 문항도 실측 **7개**(사용자는 9개로 기억).
★★**PAR-Q+ 는 쓸 수 없다**(eparmedx.com 실측 2026-09-16): 공식 번역판 10개 언어에 **한국어 없음**(「진행 중」) · 약관 「문항을 어떤 식으로도 바꾸지 말 것」 · 「상용 제품·앱 탑재는 서면 승인」 · 번역도 사전 승인. 지금 라이브의 우리 번역 7문항은 엄밀히 이 약관 밖이었다.
★**무료 대안 실측** = 호주 ESSA·SMA·AUSactive **APSS v2(2019)** — PDF 4장 전문을 뽑아 봤는데 저작권·수정금지·승인 문구 **0줄**(면책 고지만). 1단계 6문항 · 규칙 「하나라도 예 → 전문가 상담 후」. + **ACSM 2015 사전참여 알고리즘**(학술지 공개 논리 — 징후·의사 지시 → 의학적 확인 권고 / 알려진 질환·무증상 → 가벼운 강도로 시작). 국민체력100 문진은 라이선스 미확인 + 결국 PAR-Q 번역이라 제외.
🚫 PAR-Q+ 2·3페이지식 질환별 하위 문항(30여 개)은 **안 넣었다** — 8문항 성격 분류로 같은 결과가 나오고, 문항 30개는 09-12 「많이 구분하면 귀찮아한다」와 충돌.

### 한 것
- **`safety-screen.ts` 신설**(순수 · 테스트 11) — `SAFETY_QUESTIONS` 8개(문구 + `kind`) · `safetyTier(답)`. 🔴 flag(심장/뇌졸중 · 가슴 통증 · 어지럼/실신 · 천식 발작 · 혈당 조절 · 의사 관리 지시) 하나라도 예 → **2**(숨 고르기 + 꼭 상의) / 🟡 caution(관절·허리 · 기타 만성질환·처방약) 만 예 → **1**(낮은 강도) / 전부 아니오·미답 → **0**. ★**개수를 세지 않는다** — caution 둘이어도 1. `PARQ_TIER_AT`·`parqTier`·`PARQ` 배열 삭제.
- `resolveSuggestion`·`parqNotice` 는 **단계를 받는다**(개수를 받아 다시 계산하지 않음 — 정의처 하나). 화면은 `v.parqYes`(개수) → `v.tier` 로.
- 문구: 온보딩 장 제목 「PAR-Q+를 통해 …」→「몸 상태를 잠깐 여쭤볼게요」 · 본문 「캐나다운동생리학회 … 일곱 가지」→「국제 사전운동 문진(호주 APSS · 미국 ACSM)을 참고한 여덟 가지」 · 하단 안내 · 설정 「PAR-Q+ 7문항」→「안전 확인 8문항」 · 설정 상태에 **「숨 고르기만」** 추가(전엔 2단계가 「낮은 강도」로 뭉개져 보였다) · `PARQ_REST_NOTICE` 「항목이 여러 개예요」→「몸 상태를 먼저 살펴야 할 항목이 있어요」(🚫 하나여도 2단계라 「여러 개」는 거짓이 된다).
- ★**소스 가드**(jsdom 없음): `WellnessApp.tsx`·`data.ts` 코드(주석 제외)에 「PAR-Q」·「캐나다운동생리학회」 문자열이 되살아나면 실패. 식별자 `PARQ_*` 는 화면에 안 나오므로 허용.
- 옛 개수 사양을 못박던 테스트(「예 1개 vs 7개」·`PARQ_TIER_AT`)를 단계 사양으로 교체.

### 검증
tsc 0 · **111/111**(100 → +11) · build · ⚠**뮤테이션 3/3 검출**(대조군 111/111 → ①화면 문자열에 PAR-Q+ 되살림 1실패 ②caution 합산으로 2실패 ③flag 를 1단계로 2실패 → 원복 111/111) · ★**실화면(dev 3010)**: 문항 8개 · 페이지 텍스트에 PAR-Q·캐나다 0건 · **6번(징후)만 예 → 판정문 「숨 고르기만」·홈 「앉은 자리에서 이완 호흡」·설정 「숨 고르기만」** · **7·8번(조심 둘)만 예 → 「낮은 강도」·홈 저강도판 「퇴근 전 목소리·숨 고르기」·라이브러리 10가지·설정 「낮은 강도」**.

### ⚠ 남은 것
- 문항 문구·판정 매핑은 **정식 발매 전 운동처방 전문가 감수**(무료 문진이라는 것과 우리 매핑이 맞다는 것은 다른 문제). APSS 는 자유 이용을 「명시」한 것도 아니라 발매 전 ESSA 에 한 줄 확인 권장.
- 배포 **`dpl_FaaL26KCkoMyWpxoPrSCzTgKB9yB`**(READY · 별칭 `wellness-care-zeta.vercel.app` = `gbgkucxyv` 일치) · 라이브 청크 「PAR-Q」·「캐나다운동생리학회」 **0건**, 「천식 발작」·「호주 APSS」·「숨 고르기만」·「안전 확인 8문항」 각 1건.
