# 2026-09-25 웰니스 케어 워크로그

## 1건차 — 「오늘, 어떤 하루였나요?」 음성 입력이 한 단어씩 끊겨 가던 것

- 사용자: "챗팅에서 마이크를 활성화 시켜서 말하면 이어지지 않고 한단어씩 끊겨"
- 원인: 20건차(`34ad5e4`)에서 continuous:true 로 바꿨는데 전송은 예전대로 **isFinal 순간**에 했다. continuous 에서는 인식기가 단어 한두 개마다 조각을 끝내(특히 안드로이드 크롬 — 폰 실측 아님) 첫 단어만 가고, 답을 기다리는 동안 이어 한 말은 **버렸다**(`if (waitingRef.current) return;`).
- 고친 것
  - `speech.ts`: `SEND_PAUSE_MS=1500` · `heardSince(results, from)`(안 보낸 조각을 이어 한 문장).
  - `WellnessApp.tsx`: 조각을 모아 입력칸에 보여 주고 **말이 1.5초 멈추면 한 번에** 보냄(`armSend`). 답 쓰는 중에 한 말은 버리지 않고 모아 두었다가 답이 온 뒤 보냄. 브라우저가 세션을 끊고 다시 start 해도(결과가 0부터) 못 보낸 말을 `carryRef` 로 이어 붙임(`newSession`). 인식기는 여전히 하나(허용 창 반복 방지 유지).
  - 🚫 isFinal 순간 전송으로 되돌리지 말 것 · 🚫 답 쓰는 중 발화를 버리지 말 것(가드).
- 검증: RED 4 선확인 · **285 tests** · tsc 0 · build · 뮤테이션 2/2(멈춤 0ms · newSession 누락) · **실화면 가짜 인식기 + 가짜 시계**(숨은 탭 타이머 지연 제거): ①단어별 isFinal 4개 → 1.5초 뒤 한 문장 1건 ②답 쓰는 중 「그리고 사실은」 → 답 온 뒤 한 건 ③세션 재시작 걸친 「비가 / 와서 우울했어요」 → 한 문장 ④1초 쉼은 같은 문장 ⑤인식기 1개 ⑥끄기 정상.
  - ⚠ 첫 실화면 시도는 숨은 탭 타이머 지연으로 순서가 뒤섞여 판정 불가 → 가짜 시계로 다시 잼. 그 과정에서 `newSession` 이 `upToRef` 를 안 되돌리던 결함을 코드 읽기로 잡아 고침.
- ⚠ 폰 미확인: 안드로이드 크롬 continuous 가 앞 글자를 누적해 되풀이 주는 경우가 알려져 있다 — 폰에서 「오늘 오늘 하루」처럼 반복되면 다음 건차.
- 커밋 `8f911c9` · 배포 **`dpl_BP4BLTDeqdCCZmNe8tS8Aia7Wauq`**(READY · 커밋본만 담은 worktree 에서 — 미커밋 risk.ts 제외 · 별칭 `wellness-care-zeta.vercel.app` dpl 일치 · 라이브 청크에 1.5초 뒤 전송 코드 실재 · isFinal 0건) · 푸시됨.
- ▶ 눈확인(폰) = 마이크 켜고 한 문장 → 말풍선 하나 · 답 쓰는 중 이어 말한 것도 답 뒤 전송 · 앞 글자 반복 여부.

## 2건차 — 폰에서 「오늘오늘오늘오늘 오늘 하루 오늘 하루 힘들었어 오늘하루 힘들었어」

- 사용자 폰 실측(1건차 배포 뒤): 「오늘 하루 힘들었어」 한 번 말했는데 위처럼 나옴 = 1건차에서 예고한 **안드로이드 크롬 누적 조각**(조각마다 앞 말까지 포함해 다시 준다). 1건차의 `heardSince` 가 조각을 전부 이어 붙여 반복이 드러났다.
- 고친 것: `speech.ts` `mergeHeard(parts)` — 새 조각이 지금까지 이은 말로 시작하면 바꿔 끼움 · 이미 안에 있으면 버림 · 아니면 이어 붙임(공백 무시 비교). `heardSince`·앞 세션 carry 잇기 둘 다 이것을 씀.
  - ⚠ 대가: 일부러 되풀이한 말(「좋아 좋아」)은 하나로 접힐 수 있다.
- 검증: RED 4 선확인(사용자 폰 실제 모양 그대로 픽스처) · **289 tests** · tsc 0 · build · 뮤테이션 1/1(바꿔 끼우기 제거 → 2 실패).
- 커밋 `e4315c1` · 배포 **`dpl_9jjXT7EQJ3X9DG5CXZk5JBtQ95aR`**(READY · 커밋본 worktree · 별칭 dpl 일치 · 라이브 청크 `0cv2g8r3dc_44.js` 에 누적 조각 바꿔 끼우기 코드 실재) · 푸시됨.
- ▶ 눈확인(폰) = 「오늘 하루 힘들었어」 → 말풍선 하나 · 반복 없음.

## 3건차 — "자연스럽고 부드럽게" = 말하는 중 글자가 튐 · 보내기까지 너무 느림(사용자 선택)

- 튐: 안드로이드는 확정 전 글자를 줄였다 늘렸다 준다 → `steadyText(shown, next)` 입력칸은 뒷걸음치지 않음(앞부분으로 줄어든 것만 무시 · 고쳐진 글자는 바로). **화면 전용** — 보내는 말은 인식기 그대로(`heardRef`).
- 느림: 인식기가 확정(isFinal)했으면 `QUICK_SEND_MS=600` 뒤 전송 · 확정 전 멈춤은 `SEND_PAUSE_MS=1500` 그대로(생각하는 멈춤에 안 끊김) — `sendDelay(lastIsFinal)`.
- 검증: RED 4 · **294 tests** · tsc 0 · build · 뮤테이션 2/2 · 실화면 가짜 인식기+가짜 시계: 입력칸 `오늘→오늘 하→(오늘 무시)→오늘 하루→오늘 하루 힘들→(무시)→오늘 하루 힘들었어` · 확정→전송 **600ms** · 확정 없는 멈춤 1초엔 안 보냄 · 마지막 말→전송 1500ms · 인식기 1개.
- 커밋 `79dc2f7` · 배포 **`dpl_7h2mFVPufm89QrEdGqmkwf7iPfaU`**(READY · 커밋본 worktree · 별칭 dpl 일치 · 라이브 청크 `0-o0um5h43kl2.js` 에 `?600:1500` 실재) · 푸시됨.
- ▶ 눈확인(폰) = 말하는 중 글자 안 튀는지 · 다 말하고 곧 전송되는지(느리면/빨리 끊기면 QUICK_SEND_MS 한 값).

## 4건차 — 「졸립다 자야겠다」가 「졸립다」 + 「졸립다 자야겠다」 두 번 감

- 원인(폰 모양 추정 · 2건차와 같은 누적 구조): 잠깐 멈춤에 「졸립다」가 확정 → 0.6초 뒤 전송 → 안드로이드가 다음 조각에 **이미 보낸 「졸립다」까지 붙여** 「졸립다 자야겠다」를 줌 → 보낸 위치 다음 조각을 새 말로 읽어 한 번 더 전송.
- 고친 것(`speech.ts`): `dropSentPrefix(heard, lastSent)` 보낸 말로 시작하면 떼어 냄(공백 무시) · `sentMemoryAfter(heard, lastSent)` 보낸 말 기억은 **상관없는 새 말이 올 때** 잊음(인식 세션이 바뀌어도 유지 — 안드로이드는 확정마다 세션을 새로 열면서도 앞 말을 붙여 줌). 앱은 떼어 내기 전 전체(`rawRef`)를 carry·lastSent 로 잡는다(뗀 뒤 값으로 잡으면 다음 누적과 안 맞는다 — 착수 중 발견).
- 결과: 「졸립다」 → (답) → 「자야겠다」. ⚠ 멈춘 순간 앞 문장이 이미 갔으므로 **말풍선은 둘**(한 말풍선으로 묶으려면 기다림을 늘려야 하고 그러면 느림이 돌아온다 — 사용자에게 고지).
- 검증: RED 선확인 · **301 tests** · tsc 0 · build · 뮤테이션 1/1 · 실화면 가짜 인식기(안드로이드 누적·세션 재시작)+가짜 시계: 같은 세션 `졸립다 → 자야겠다 → 그리고 씻어야지` · 세션 재시작 `배고파 → 밥먹자` · 상관없는 말 뒤 「졸립다」 다시 → 그대로 전송 · 인식기 1개.
- 커밋 `618d652` · 배포 **`dpl_CtxHWbhRGeP5NuQ4YcbaHZoV2xqw`**(READY · 커밋본 worktree · 별칭 dpl 일치 · 라이브 청크 `2ej14e5sa6z-c.js` 새로 바뀜) · 푸시됨.
- ▶ 눈확인(폰) = 「졸립다 … 자야겠다」 → 「졸립다」·「자야겠다」 두 말풍선, 중복 없음.
