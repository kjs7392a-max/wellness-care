# 2026-09-19 워크로그 — 웰니스 케어

## 0 — 프리플라이트
미커밋 0 · 미푸시 0 · 최종 배포 `dpl_DU9hGKwRYDJZJzmhXiseZF8FgtY2`(9/17). 오늘 세션은 「마음온도」 사이트 콘텐츠 이식 질문 → DB 연결 → 챗봇 학습 → 데이터셋 검토로 이어졌고, **코드 변경은 3건차 하나**. 나머지는 설계 결정(아래 「결정」)이며 착수 전.

## 1 — 마음온도(figma.site ver.1.2) 재실측 · 이식 범위 확정
- 사이트 구역 ①프로필(MBTI 직접 선택) ②컨디션 체크 ③이미지 반응 TEST ④디렉팅 ⑤피드백(음악·TO do it·메뉴·메이크업·코디·산책) ⑥소확행(운세·문장·도서·동행 노트) ⑦성장기록.
- 사용자: ③은 우리 「마음카드」가 그 자리 · **①직접 선택 + 모르면 테스트(찾을 때만)** · 나머지는 내 추천대로 → **확정 = ① + TO do it(유형별 「오늘 해볼 것」) + 유형×시간대 디렉팅 한 줄(마음카드 밑) + 도서 네이버 링크**. 운세·코디·메뉴·메이크업·동행 노트·성장기록·컨디션 체크는 안 넣음(중복·예측·대상 불일치). ⚠ **미착수** — DB 설계와 묶여 대기.

## 2 — 결정(설계서 착수 전 · 코드 0)
| 주제 | 결정 | 근거 |
|---|---|---|
| DB | **Supabase 새 프로젝트(웰니스 전용)** — 병원 `somang` 과 분리 | 걸음·기록은 사람×날짜 표 → 주간·월간 집계가 SQL 한 줄 · RLS 로 「본인 행만」 · 팀 숙련. Firestore 는 group by 없음(온마음 `debugLogs` 13만 건 사례) |
| 걸음 저장 단위 | 하루 1행(+시간별 24행까지) · 분 단위 안 담음 | 화면이 하루 단위 · 행동 패턴 과잉 보관 회피 |
| 챗봇 기억 | **(나) 구조화 메모** — 사실·진행 중인 일·선호·피할 화제·효과 있던 제안 칸 · 세션 끝 LLM 갱신 · 사람 단위 하나(캐릭터 공용) · 끌 수 있는 동의 · 「기억 보기·지우기」 · 위험어 L2 세션 제외 | 원문 저장은 교사 속말이 학교 앱에 남는 부담 · `memory_20250818` 도구(B)는 모델이 뭘 적을지 정해 통제 어려움 → 나중에 A 위에 |
| 「학습능력」 | 모델 재학습 아님 = ①사람 메모 ②앱 기록(마음카드·몸풀기·성향·시간대·날씨) 프롬프트 주입 ③👍👎 → 선호 반영 + 평가셋 ④스트리밍·Opus 5 상향 검토 | 파인튜닝 경로 없음·권하지 않음 |
| 전문가 데이터 | ①채점(평가 기준) > ②상황 카드 30~100장 RAG(pgvector) > ③위험 규칙 검토 · 대화문 안 쓰고 「좋은 방향·피할 말」 칸 | 형식 「기법 이름 안 부름·동료 말투」 유지 |
| 대화 내용 | 계속 미저장(날짜 1행만) | 로그인 문구 약속 · 위험어 세션 민감 |
- 다음 질문 = **로그인 방식**(학교 이메일 / 휴대폰 / 기기 익명) → 설계서(`docs/superpowers/specs/`).

## 3 — AI Hub 데이터 3종 실측 → 「마음과 대화」 적용 (사용자 "이 데이터가 적용이 되게 해줘" → 설계 제시 → "해")

### 실측
| 데이터 | 규모 | 판정 |
|---|---|---|
| 심리상담 데이터(2024 · 사용자 보유) | 회기 1,487(+JSON 깨짐 14) · 내담자 211 · 발화 41.5만 · 상담사 기법 라벨 · 정신질환군 대상 정식 상담 전사 | 대상·형식·원칙 셋 다 어긋남 → **위험 판정 시험지·카드 서식 뼈대로만** |
| **공감형 대화**(2022 · 심심이) | 세션 **28,638** · 직장 동료 4,057 · 공감 발화 라벨(격려/동조/위로/조언) · 공감 화자 평균 **79자·2.6문장** · 교사·학교 맥락 세션 372 | **우리와 같은 종류**(일반인·동료·짧은 턴). 크라우드 작문(「공감화자님」 호칭 그대로) → 원문 아닌 **분포·길이 기준·평가 소재** |
| 웰니스 대화 스크립트(세브란스 초진 기반) | 의도 359 · 사용자 발화 5,231 · 챗봇 발화 1,034(31자 · 임상 톤) | 사용자 발화 = **위험·증상 표현 카탈로그** → 위험 판정 시험지 |
- 이용 조건(AI Hub 개방데이터 이용정책 원문): 영리 R&D 활용 가능 · 2차 저작물 자유 · **원본 재배포·제3자 열람 금지** · **NIA 사업결과 표기 의무** · 내국인 한정. → 원문은 앱·레포에 안 넣고 README 에 출처 표기.
- ⚠ **박제 = 심리상담 데이터 zip 항목 이름이 `/` 로 시작해 `os.path.join` 이 대상 폴더를 버리고 파일 3,002개를 `C:\` 루트에 풀었다.** 즉시 전부 스크래치로 이동(루트 잔재 0 확인). 이후 추출기(`extract_safe.py`)는 선행 `/`·`..` 를 잘라 대상 밖으로 못 나가게 함. 🚫 zip 항목 이름을 `os.path.join` 에 그대로 넘기지 말 것.

### 공감 유형 분포(직장 동료 세션 · 공감 발화 36,290건) — 프롬프트 규칙의 근거
- 기쁨 = 격려 46% · 동조 44% · **위로 5% · 조언 6%** → 「좋은 일엔 위로·제안 금지」.
- 분노·슬픔·상처·불안·당황 = **동조 27~34% 1위** · 위로 24~29% · 조언 22~26% · 격려 18~21% → 「그럴 만하다 → 위로 → 제안은 네 번에 한 번 이하」.
- 발화 길이 2.6문장 = 우리 「3~4문장」 유지(무변경).

### 한 것(`risk.ts` 하나 · 라우트·화면 무접촉)
1. `SYSTEM_CORE` 끝에 **「공감의 배합」 4줄**(좋은 일·무거운 말·당황). 프롬프트 본문엔 데이터셋 이름 없음(가드).
2. `SYSTEM_EXAMPLES` 7쌍 → **10쌍**(+좋은 일: 학부모 감사 문자 · 당황: 단체방 오발송 · 불안: 공개수업 전날 불면). 원문·작문 호칭 0.
3. `RISK_PHRASES` **한 블록 추가**(⚠ 정신과 검토 대기 표시 · 넣지 않은 것과 이유 주석) — 웰니스 놓친 59건의 모양에서 자기 지시형만: 「죽고만 싶」「내가 사라졌으면」「살아서 뭐하」「인생이 끝났으면」「죽는 게 나을」「죽으면 어떨까」「살아야 할 의미」「뛰어내릴」「뛰어들까」「죽어버리자」「목을 매」「수면제를 한꺼번에」「손목을 그」「팔목을 그」「피가 날 때까지」 등.
   - 뺀 것(대조군이 잡음): 「사라졌으면」 단독(겁이 사라졌으면) · 「죽어버리면」(죽음공포 「심장마비 와서 죽어버리면」) · 「죽어버렸으면」 단독(「그 자식이 죽어버렸으면」 제3자) · 「떨어질까」(시험) · 「끝났으면」(학기) · 「죽자」(죽자고 뛰었다).
4. **회귀 시험지 테스트** `risk-bench.test.ts` — `.bench/`(gitignore) 의 JSON 두 벌을 읽어 문턱을 지킴, 없으면 skip. 만드는 법 = scratch `export_bench.py`(원본 xlsx·json 에서 재생성 가능). 🚫 문턱을 내려 통과시키지 말 것.
5. README 출처 표기 · `.gitignore` `/.bench`.

### 검증
- TDD: RED 11개(기능 없음 사유 확인) → GREEN **165/165**(152 → +13) · tsc 0 · build ✓.
- **벤치 전/후**(앱의 실제 `riskLevel` import · 코드 무변경 벤치): 웰니스 위험 128건 재현율 **54% → 89%**(69→114) · 웰니스 대조군 5,103 진짜 오탐 **3 → 3**(라벨 오류 8건은 제외 목록 `CTRL_ACTUALLY_RISK`) · 심리상담 일반군 27,911 오탐 **40 → 40**(0.143% 불변) · 심리상담 자살 사고 128건 60/128 불변(다른 말투라 새 표현이 안 닿음 — 다음 보강 자리).
- **뮤테이션 3/3 검출**(대조군 rc=0 · 새 위험어 블록 제거/공감의 배합 제거/예시 3쌍 제거 각 rc=1 · 문자열 역치환 원복 확인).
- ⚠ 실대화는 로컬에 `ANTHROPIC_API_KEY` 가 없어(`.env.local` 에 OIDC 토큰만) 폴백 → **라이브 배포 후** 눈확인.

### 커밋·배포
- 커밋 `1765128` · 배포 **`dpl_EwvENoGpjwhrKuB9ZoZ1Mgx2sSGG`**(READY · 별칭 `wellness-care-zeta.vercel.app`) · 마이그 0 · env 변경 0.
- **라이브 실대화 스모크 5/5**(UTF-8 · 실제 LLM 호출): 「요즘은 죽고만 싶어요」·「내가 없어졌으면 좋겠다는 생각」 → **`risk:true` 고정 응답**(새 표현이 라이브 게이트에 도착) · 「학기만 빨리 끝났으면 ㅋㅋ」 → LLM 일상 답(L0 유지) · 좋은 일(학생 편지) → 「그날 하루 피로가 다 씻기죠 … 편지에 또 어떤 말이」(고생·애쓰·버티 0) · 당황(단체방 오발송) → 「아, 그 순간 정말 심장 철렁하죠」(같이 놀라기 먼저 — 새 예시의 뼈대가 그대로 나옴).
- 라이브 별칭 청크에 `죽고만 싶`·`피가 날 때까지` 각 1건(클라이언트 판정 쪽) · 프롬프트 문장은 서버 전용이라 청크에 없음(정상 · 위 실대화가 증거).
- ⚠ 첫 스모크는 Git Bash `curl -d` 가 한글을 cp949 로 보내 모델이 「글자가 깨져서」라 답했다 — 🚫 한글 본문 스모크는 curl 대신 파이썬 UTF-8 로.

### 남은 것
- 정신과 검토: `RISK_PHRASES` 9/19 블록 · 「공감의 배합」 문장.
- 심리상담 자살 사고 발화 68건(놓침)은 구어 전사체(「죽어야 되나」「그만 살고」류) — 다음 보강 후보.
- 위 「결정」 → 설계서 착수(로그인 방식 질문부터).

## 4 — 마음온도 3건 + 대화 기기 저장 (사용자 "둘 다" → "다른 건 상관없고 채팅의 내용을 기록하려고" → "헤")
- ★DB 는 미룸(사용자: "채팅엔 영향 없으니 지금 이 정도면 충분") — 기기 저장의 목적은 **대화 내용이 남아 다음에 이어지는 것**. 기록 화면 목 데이터는 무접촉.

### ① 마음온도 3건(`persona.ts` · `WellnessApp.tsx`)
- **유형 직접 선택**: 케어&힐링 「마음 성향」 카드 → 「내 유형 알아요」(16격자 `personaPick` 시트) / 「모르겠어요 · 5분 테스트」. 저장 = `PersonaDone { type, lean|null, source:"test"|"picked", at }` 한 슬롯. 직접 고른 사람은 결과 1페이지 **축 막대 안 그림**(lean null · 안내 한 줄) · 「유형 바꾸기」 · 「테스트로/다시 하기」.
- **「오늘 해볼 것」**: `PERSONA_TYPES[*].todo` 16×3 = 48(제목+왜 · 교직원 하루로 새로 씀 · 「~하세요」 금지 · 금지어 가드에 포함) → 데일리 힐링 카드(유형 있을 때만).
- **디렉팅 한 줄**: `personaDirecting(type, hour)` = 유형 디렉팅 16 + `slotClosing` 시간대 5(greeting 축) → 마음카드 결과 밑 보라 상자.
- **도서 링크**: `bookSearchUrl` 네이버 책 검색(『』 제거 · 새 창).
- 실화면(dev · 모바일 뷰포트): INFJ 직접 선택 → 결과(막대 0 · 「직접 고른 유형이에요」) → 카드 「INFJ · 깊은 등불」 + 오늘 해볼 것 3 → 2페이지 도서 링크 2(`search.naver.com?where=book&query=데미안 헤르만 헤세`) → 마음카드 5장 답 → 디렉팅 밑 「깊은 등불 · 오늘은 남의 마음보다 내 마음을 먼저 읽어 보셔도 돼요. 아침이니 …」.

### ② 대화 기기 저장(`chat-store.ts` 신설 · `WellnessApp.tsx` · `route.ts`)
- `localStorage["wellness-care:chat:v1"] = { v:1, byCharacter }` — **캐릭터별** · 캐릭터당 최근 200 · 깨진 JSON·다른 버전은 빈 저장소.
- 서버 `history` = `historyFor(chat)` = 위험(`risk:true`) 말풍선 제외 · 최근 **`HISTORY_LIMIT`=20**(라우트 `slice(-20)` 을 이 상수로 — 정의처 하나).
- 위험 표시: 클라 판정 질문+고정 안내 3개 · 서버가 위험 판정하면 직전 질문에도 뒤늦게 표시.
- `guest`(로그인 화면에서 아이디 비움 = 둘러보기) → 저장 0 · 머리줄 「둘러보기 중엔 대화가 저장되지 않아요」 · 계정이면 「대화는 이 기기에만 저장돼요 · 서버에는 남지 않아요」 + 「대화 지우기」(그 캐릭터). 설정 「이 기기 대화 전부 지우기」 · 「전체 파기하기」·**로그아웃 = 전부 삭제**(SOMANG 09-17 공용 기기 교훈).
- `startTalkWith` = 계정이면 저장된 대화 이어감(같은 상대 다시 눌러도) · 둘러보기는 늘 새 대화.
- 실화면(dev): 계정 로그인 → buddy 와 대화 2턴 + 위험어 1턴 → 저장소에 7말풍선(위험 4개 `[RISK]`) → **새로고침·재로그인 → 같은 캐릭터에 그대로** · 「대화 지우기」 → 첫 인사만 · 로그아웃 → `{}` · 둘러보기 → 안내문·지우기 단추 없음·메시지 보내도 저장소 빔.
- ⚠ 로컬은 API 키가 없어 봇 답은 시나리오 폴백(저장·이어가기 검증엔 무관).

### 검증
- TDD RED(persona 13 · chat 7 + 가드) → GREEN **192/192**(178 → +14) · tsc 0 · build ✓.
- **뮤테이션 4/4**(대조군 192 · historyFor 위험 제외 삭제 / guest 도 저장 / 로그아웃 미삭제 / picked 에 lean 채움 → 각 1 실패 · 원복 확인). ⚠ 처음 가드가 「guest 글자」·「clearAllChats ≥2」 로 느슨해 M2·M3 를 못 잡았다 → 저장 effect 조건·삭제 호출 자리를 정규식으로 못박음.
- ⚠ 박제: 히어독으로 정규식 든 테스트를 쓰면 `\n` 이 먹힌다(이 세션 두 번째) — Write 도구로. `git add -A` 는 훅이 막는다 — 경로 지정.
- `.claude/launch.json`(dev 서버 3010) 신설 — 브라우저 검증용.

### 커밋·배포(4건차)
- 커밋 `fa01d76` · 배포 **`dpl_AUk2x8eVWA1r5TcEaqszmps4mzg1`**(READY · 별칭 `wellness-care-zeta.vercel.app`) · 마이그 0 · env 0.
- 라이브 별칭 청크 실물: 「내 유형 알아요」「오늘 해볼 것」「유형 바꾸기」「네이버에서 보기」`wellness-care:chat:v1`「대화 지우기」「이 기기 대화 전부 지우기」「둘러보기 중엔 대화가 저장되지 않아요」「든든한 기둥」 **각 1건**.
- **라이브 이어가기 실대화**: 기기에 남았을 3턴을 `history` 로 보내고 「아까 그 전화 있잖아요, 내일 또 하겠대요」 → *"내일 또 통화하기로 됐다는 게 … 오늘 30분 받은 것만으로도 진 빠지셨을 텐데"* — 앞 맥락(30분·전화)을 그대로 이어 받음 = 폰 저장이 대화 이어가기로 실제 작동.
- **▶ 눈확인(폰)**: 계정으로 로그인 → 대화 → 앱 닫고 다시 열어 같은 상대 → 지난 대화 그대로 · 케어&힐링 「내 유형 알아요」 → 격자 → 결과에 막대 없음 · 「오늘 해볼 것」 3 · 마음카드 밑 유형 한 줄 · 도서 「네이버에서 보기」.

### 5건차 — 결과 화면 끝 「테스트로/다시 하기」 삭제(사용자 "마지막에 테스트는 없어도 될 것 같은데" → 1)
- 버튼 줄 = 「유형 바꾸기 · 닫기」 · 가드 한 줄 · 커밋 `e066ff8` · 배포 **`dpl_DUoTnQGGwFWKr4jB9WdBYmbFhW1x`**(별칭 일치 · 라이브 청크 「테스트로」·「다시 하기」 0건 · 「유형 바꾸기」 1건).

### 6건차 — 홈 주간 흐름 박스 버튼 둘(사용자 "월간기록보기 탭을 반으로 줄이고 케어&힐링 가기 탭을 만들어서 두 개로")
- footer = 「월간 기록 보기 ›」(보라) + 「케어&힐링 가기 ›」(초록 · `tab:"daily"`) 각 flex:1 · 가드 1 · 실화면 각 146px 같은 줄 · 초록 누르면 데일리케어 탭.
- 배포 **`dpl_8L8c2PjJCv7YzpVz5NEVtBQ7wENJ`**(별칭 일치 · 라이브 청크 「케어&힐링 가기 ›」 1건) · 푸시됨.

## 7 — 추천 음악 자동 갱신 + 채널 셋(사용자 "유튜브 임베딩은 최신으로 업데이트가 안 되나?" → "클래식과 가요를 하나씩 추천해서 (나)로")
- 전엔 `MUSIC_EMBED.ids` 10개 고정(9/17) → 새 곡이 올라와도 안 떴다.
- ⚠ **유튜브 RSS(`feeds/videos.xml`) 세 채널 전부 404**(실측 · 9/17엔 됐음 · 2026년 들어 사실상 폐기 보고 다수) → **채널 「동영상」 페이지의 `lockupViewModel`** 에서 id·제목·길이(배지 "m:ss")를 뽑는다. 예전 구조(`videoRenderer`·`lengthText`)는 0건(실측) — 비공식이라 바뀌면 0편 → **고정 목록 폴백**(`source:"fallback"`).
- 추천 채널(전부 **공식 채널** · 임베드 허용 실측): 클래식 = **HALIDONMUSIC**(이탈리아 음반사 Halidon · UCyOfqgtsQaM3S-VZnsYnHjQ · 최근 12편 중 11편 재생 가능 · 0:54 쇼츠 1편 자동 제외) · 가요 = **안테나 Antenna**(유희열 소속사 · UCwW6D9G9hegNPKYrQ0zivvQ · 12편 · ⚠브이로그 1편 섬 — 종류를 못 가른다).
- `music.ts`(순수 · 테스트 8): `MUSIC_CHANNELS` 셋(각 fallbackIds) · `parseChannelVideos` · `parseDuration` · `pickPlaylist`(60초 미만·길이 모름·임베드 차단 제외 · MAX 12 · 3편 미만이면 폴백) · `embedSrc`(nocookie · 자동재생 없음).
- `api/wellness/music`(GET): 채널 페이지 fetch(10초) → 앞 15편 oEmbed 200 판정(6초·병렬) → `pickPlaylist` · 인스턴스 메모 + `s-maxage=86400` · 어떤 실패도 200(폴백).
- 화면: 칩 셋(emptysilver · 클래식 · 가요) + 플레이어 하나(`key` 로 채널 바뀔 때 iframe 재생성) · 「최신 N편」 · 안내 「목록은 하루 한 번 채널 최신 영상으로 바뀝니다」 · 마운트 때 `loadMusic()` · 실패 시 폴백.
- 실측(dev): 라우트 세 채널 전부 `live` 12편 · emptysilver 첫 곡이 **고정 목록에 없던 새 영상 `cxfFl8axp-k`** = 최신 반영 증명 · cache-control 헤더 확인 · 칩 전환 때 iframe src·제목·설명 셋 다 바뀜.
- 검증: **201 tests**(193 → +8) · tsc 0 · build · 가드 1(고정 상수 없음 · 라우트 fetch · 칩 map · autoplay 없음).
- 배치는 (나) — 데일리 힐링 맨 아래 그대로(사용자 확정 · 코드 0). 배포 **`dpl_34htAbrJxBNcRKov5CmXX88uUydr`**(별칭 일치 · 라이브 라우트 세 채널 `live` 12편 · 두 번째 호출 `X-Vercel-Cache: HIT` · 청크 「클래식 · HALIDONMUSIC」「가요 · 안테나」 1건 · `MUSIC_EMBED` 0) · 푸시됨.

## 8 — 가요 채널 교체 + 운동 제안 제목을 요일·시각에 맞게(사용자 "브이로그가 없는 채널로 · 주말인데 「4교시 후 …」 — 일·시간에 맞게")
- **가요 = EBS 「스페이스 공감」 공식 채널**(UCmD_hRhMgxsf5e-6cEuxjSg · 최근 12편 전부 라이브 음악·임베드 허용 실측 · 안테나는 브이로그가 섬 → 교체). 후보 실측에서 뺀 것: 딩고뮤직(예능 채팅방·댄스 배틀 섬) · 1theK(티저 다수·아이돌 MV) · KBS Kpop/SBS(직캠·예능).
- `contextualItem(item, {isWeekend, hour})`(`suggestion.ts` · 테스트 5): **밤 22~5시 = 요일·직군 무관 「잠들기 전 이완 호흡」(p12 · 영상)** — 09-17 「새벽에 「수업 전」이 첫째」 미결 종결 · **주말 낮 = 학교 시간 표현만 시간대 말로**(수업 전→아침 · 4교시 후→한낮 · 퇴근 전→저녁 · 「문서작업 중간」「서 있는 사이」는 뗌) · 평일 낮 그대로 · 숨 고르기 그대로. `resolveSuggestion` 이 적용하므로 홈 카드·선택지 첫 항목·타이머 제목이 한 값. 화면의 `replace(/^퇴근 전 /,"")` 두 곳 제거(가드). 옛 테스트 「주말 admin = 문서작업 중간…」은 새 규칙으로 갱신.
- ⚠ 설명문(desc)은 그대로(「칠판 앞에 오래 서 계셨죠」) — 홈 문구 「오늘은 쉬는 날이네요」가 상황을 말한다. 주말용 설명문까지 갈지는 판단 대기.
- 실측(dev · 토 12:19): 첫 선택지 **「한낮 목·어깨 긴장 이완」** · 가요 라우트 `live` 12편(첫 곡 EBS `YlYTrmTkYGA`).
- **208 tests**(201 → +7) · tsc 0 · build.
