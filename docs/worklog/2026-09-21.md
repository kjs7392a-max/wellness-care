# 2026-09-21 워크로그 — 웰니스 케어

## 0 — 프리플라이트
미커밋 0 · 미푸시 0 · 최종 배포 `dpl_B8banxp4b8JHF6uxzBmEZEt5aLEZ`(9/19 12건차). 오늘은 1건.

## 1 — 홈 인사말 아래 30문장 랜덤(사용자 *"이 문장들은 랜덤으로 넣어줘"*)
- 발단: 사용자가 「오늘 하루도 고생이 많으셨어요」(= `greetingForHour`, 시간대 5구간 한 줄) 아래에 붙일 30문장을 줌 — 출근·등교 전 10 · 퇴근 후 15 · 주말·휴일 5.
- 확정 2건(착수 전 질문): **평일 낮 11~18시 = 30개 전부에서**(원문에 그 시간대 묶음이 없다) · **랜덤 주기 = 열 때마다**(내 추천 — 하루에 하나면 30개 넣은 뜻이 약해진다).
- 구현: `homeMessage.ts` 신설(원문 그대로 · 줄바꿈 유지 · `homeMessagePool` 주말 → 평일 5~11 → 평일 18~5 → 그 밖 전부 · `pickHomeMessage(ctx, seed)` 시드 접기) · `WellnessApp` 상태 `msgSeed`(마운트 이펙트에서 `Math.random()` 한 번 · 서버 렌더 땐 null → 안 그림 = hydration 안전) · 인사말 머리줄 **아래 전폭 카드**(머리줄 안 좁은 칸에 넣으면 5~6줄이 세로로 길게 접혀 머리줄을 밀어낸다) · `white-space:pre-line`.
- 🚫 렌더 안에서 `Math.random()` 금지(가드가 막는다 · 매 렌더 문장이 바뀌어 깜빡인다).
- 검증: 신규 13 tests(순수 11 + 소스 가드 2 · 100시드 → 15문장 전부 도달) · **220 tests** · tsc 0 · build · 뮤테이션 3/3(주말 판정 제거 · 시드 접기 제거 · 렌더 안 Math.random) 대조군 무변경 통과 · dev 실화면(폰 폭 · 월 21:27 → 퇴근 후 #17 5줄 · 콘솔 0).
- 커밋 `8eadb8d` · 배포 **`dpl_Huy6AjC51J4eujCF8r6rH8CmBzVa`**(READY · production · 별칭 `wellness-care-zeta.vercel.app` dpl 일치 · 라이브 청크 `2ur0ytylxhbb9.js` 에 「쉬는 날에는」 실재) · 푸시됨.
- ▶ 눈확인(폰): 홈 인사말 아래 문장 · 앱 닫고 다시 열면 다른 문장 · 주말에 열면 주말 5개 중.

## 2 — 페이지 분할: 홈 = 오늘의 기록까지 · 「케어」 / 「디렉팅」 탭(사용자 *"네비게이션바는 케어 와 디렉팅으로 나눠줘"* → *"일단 데일리 힐링을 한페이지 만들어 주는것 까지만"*)
- 탭 `home | care | healing | my` = 홈 · 케어 · 디렉팅 · my. `renderDaily` → `renderCare`(데일리케어 제목 + 신체·마음 케어 카드 둘) + `renderHealing`(데일리 힐링 제목 + 마음 성향 · 오늘 해볼 것). 페이지 안 제목은 지시 없어 그대로(질문창은 닫혔다).
- 홈은 인사말·30문장·「오늘의 기록」까지. 그 아래 있던 **주간 흐름 박스·날씨·운동 N가지 제안은 `renderHomeMore()` 로 옮겨 보관 · 미배선**(가드가 홈에 없음 + 함수 실재를 못박음) — 🚫 지우지 말 것 · 🚫 홈으로 되돌리지 말 것. 안의 「케어&힐링 가기」 단추는 「케어 가기」(`tab:"care"`)로.
- 검증: 가드 4건(RED → GREEN) · 225 tests · tsc 0 · build · dev 실화면(홈/케어/디렉팅 세 페이지 텍스트 대조 · 콘솔 0).
- 커밋 `9c9d7b2` · 배포 **`dpl_8EQVJra6wYhoF47yM2SCQZyRGn9B`**(READY · 별칭 dpl 일치 · 라이브 청크 `0q69h4q2m0e8g.js` 에 `"healing","디렉팅"` 실재 · 「케어&힐링 가기」 0) · 푸시됨.
- ⚠ 박제: Bash heredoc 이 `\n` 을 실제 줄바꿈으로 바꿔 테스트 파일이 깨졌다(quoted heredoc 이어도) → Edit 도구로 고침. 정규식·이스케이프 든 파일은 heredoc 금지(세 번째).
- **▶ 다음 단계(지시 대기)** = `renderHomeMore` 가 들어갈 「나머지 페이지」 — 새 탭인지 · my 안인지 · 이름. **▶ 눈확인(폰)** = 하단 탭 넷 · 홈이 오늘의 기록에서 끝남 · 케어 카드 둘 · 디렉팅 = 마음 성향.

## 3 — 그래프는 홈 · 제안은 케어(사용자 *"월간기록보기 그래프는 어디로 사라졌어? 그리고 운동 제안도"* → *"그래프는 … 페이지에 붙혀주고 제안과 운동 4가지는 케어페이지에"*)
- 2건차에서 `renderHomeMore` 에 미배선으로 두었던 셋이 라이브에서 안 보이던 것. 사용자 지정대로 **이번 주 흐름 박스 → 홈 「오늘의 기록」 아래(원자리)** · **날씨 한 줄 + 「신체 건강을 위한 운동 N가지 제안」 → 케어 탭 카드 둘 아래**. `renderHomeMore` 삭제 · `daySolution` 계산은 `renderCare` 로.
- 가드 갱신(홈에 `renderWeekFlow` · 케어에 `buildDaySolution`·`v.choices.map` · 카드가 제안보다 앞 · `renderHomeMore` 0). 225 tests · tsc 0 · build · dev 실화면(홈·케어 텍스트+스크린샷 · 콘솔 0).
- 커밋 `66ac198` · 배포 **`dpl_4smhZ5dmmsNhAEZA6n5H8mX6yLmd`**(READY · 별칭 dpl 일치 · 청크에 「케어 가기 ›」 실재 · `renderHomeMore` 0) · 푸시됨.
- ⚠ 배포 훅이 `git commit && vercel` 한 명령을 통째로 막아 커밋도 안 됐다(08-13 박제 재발) → 커밋·배포 별도 명령.
- **▶ 눈확인(폰)** = 홈 그래프 복귀 · 케어 탭 아래 날씨+제안 4가지.

## 4 — 「케어 가기」 삭제 · 웰컴 문장 카드 진하고 크게(사용자 *"월간 기록보기 탭만 놔두고 케어가기는 삭제 · 상단 웰컴 문장을 조금 더 진하고 크게 밸런스"*)
- 주간 흐름 박스 단추 = 「월간 기록 보기 ›」 하나(한 줄 전체) — 케어는 하단 탭에 있다. 🚫 되살리지 말 것(가드).
- 30문장 카드 14px/500/#3f6b7c → **15.5px/600/#2d5c6e**(인사말과 같은 색). 「웰컴 문장」= 이 카드로 해석(인사말 한 줄이면 정정 요청 대기).
- 가드 2건(RED→GREEN) · 226 tests · tsc 0 · build · dev 실화면.
- 커밋 `5dc9a63` · 배포 **`dpl_5teEv3GVCiWhAz5ATkfr5nJDLx1e`**(READY · 별칭 dpl 일치 · 청크에 월간 1 · 케어 가기 0 · 새 스타일 1) · 푸시됨.

## 5 — 케어 탭 두 박스(사용자 *"신체건강 케어와 운동 4가지 제안을 한 박스로 · 마음건강 케어를 한 박스로"*)
- 신체 박스 = 옛 신체 카드 껍데기(복숭아 그라데이션) 안에 머리줄(→ 운동 목록 클릭) → 구분선 → 날씨 한 줄 → 제안(흰 반투명 판 · 배지·문구·선택 N개·미루기·안전 안내). 마음 박스 = 카드 그대로 아래. `data-box="physical|mind"` 표식(가드용).
- 가드: 순서 신체→제안→마음 · 제안이 physical 과 mind 사이. 226 tests · tsc 0 · build · dev 실화면(스크린샷 + DOM 실측 mind 가 physical 아래).
- 커밋 `7abc44e` · 배포 **`dpl_B4dkCJkHhuFVcQS8MWwpLUX5t4or`**(READY · 별칭 dpl 일치 · 청크에 `data-box="physical"` 실재) · 푸시됨.

## 6 — 케어 탭: 「데일리케어」 제목·날씨 삭제 · 마음 박스 칩 둘(사용자 *"데일리케어 제목을 빼주고 온도 산책하기 좋은 날 이런거 삭제 · 마음 케어 박스에 오늘,어떤 하루였나요? 와 오늘의 마음카드 칩"*)
- 제목·부제 블록 삭제(부제는 제목과 한 덩이라 함께 — 사용자에게 고지) · 신체 박스 날씨 한 줄 삭제(제안 문구가 날씨를 말한다 · `wx.prefer` 는 계산에 계속 쓴다).
- 마음 박스 = 머리줄(→ 마음 건강 시트) + 칩 둘: 「오늘, 어떤 하루였나요?」 보라 → `sheet:"mind"`(대화 상대 고르기) · 「오늘의 마음카드」 흰 → `sheet:"picture"` 바로. 실클릭 둘 다 확인.
- 가드(제목·`wx.label` 없음 · 칩 둘 + 목적지). 226 tests · tsc 0 · build · 콘솔 0.
- 커밋 `9cabe70` · 배포 **`dpl_JDsCJ1AF9VavP8ipHHbodPADhLa7`**(READY · 별칭 dpl 일치 · 청크에 칩 1 · 「데일리케어」 0) · 푸시됨.

## 7 — 신체 박스 부제 → 누르는 칩(사용자 *"신체건강을 위한 간단한 운동 15가지를 「더 다양한 운동 15가지를 해보세요」 누름 표시로"*)
- 머리줄 부제 삭제 · 그 아래 칩 「더 다양한 운동 {N}가지를 해보세요 ›」(신체 색 #e0876c · 마음 박스 칩과 같은 꼴 · → `sheet:"library"`). 머리줄 ↗ 도 그대로 같은 곳. N = `v.libList.length`.
- 가드(옛 부제 없음 · 칩 문구+목적지). 226 tests · tsc 0 · build · dev 실클릭(운동 목록 시트 열림 · 콘솔 0).
- 커밋 `509d10d` · 배포 **`dpl_3QDPPsG2n6koZPFonU2hwapM6xr3`**(READY · 별칭 dpl 일치 · 청크 칩 1 · 옛 부제 0) · 푸시됨.

## 8 — 디렉팅 탭 제목 「데일리 힐링」 → 「나를 위한 디렉팅」(사용자 *"일단 수정해줘"* · 제목만 · 부제 그대로)
- 커밋 `9404212` · 배포 **`dpl_6WUGwNhB4v3WduG9kT71wmShmtUA`**(READY · 별칭 dpl 일치 · 청크 새 제목 1 · 옛 제목 0) · 226 tests · 푸시됨.

## 9 — 「나를 위한 디렉팅」 탭 7개 = 마음온도 「피드백」 이식(사용자 *"mbti 선택하면 나오는 것들을 따로 한 페이지씩 · TO do it · 추천 음악 · 메이크업·코디·문장·산책은 마음온도에서 그대로 옮겨줘"* → 추천안 채택 *"그래"*)
- 실측: 마음온도(`stuff-puppy-73155246.figma.site`) 로그인→프로필→컨디션→이미지 반응→「피드백」 6칸. 번들 `_components/v2/d63f…js`(1.1MB · 미난독)에서 표·규칙을 그대로 뽑음(`scratchpad` 아닌 `%TEMP%/maeum/` 하네스 · node eval 로 표를 JSON 화).
  - 메이크업 = 4문항(질감·톤·고민·목적) → 표(원본 G2 여성 27 · K2 남성 36 · X2/Y2 고민 없음 9+12) + 톤 팁(Cb/V2) + 기온·습도 한 줄(Sp) · 코디 = iN/oN/aN/rN/nN/sN/tN/eN/wp(장면 4 × 기온 × 성별 × 나이) · 문장 = x2(상태·수면·걸음·점수·기분) · 산책 = Mo/kN(코스 3 + 지도 · MBTI 무관).
- 사용자 확정: **성별·나이대 = 코디·메이크업 탭 첫 진입 때 한 번 · 기기 저장 · 설정에서 변경** · **탭 순서 = 마음 성향 · TO do it · 추천 음악 · 메이크업 · 코디 · 문장 · 산책**(질문창은 닫혔고 "너의 추천은?" → "그래").
- 구현 `src/features/wellness/directing/` = `profile.ts`(localStorage `wellness-care:directing-profile:v1` · 나이대 대표 나이) · `makeup-data.ts`(생성 · 44KB) · `makeup.ts` · `outfit.ts`(원본 함수 이름만 바꿔 그대로) · `sentences.ts`(마음카드 기분·잠 → 원본 입력 · 걸음 5000·점수 60 고정) · `walk.ts`. `renderHealing` 칩 탭 · 유형 전엔 「마음 성향」만 · `renderHealingCards` → `renderPersonaCard`+`renderTodoCard` · 설정 「디렉팅 코디·메이크업 기준 → 다시 고르기」 · 전체 파기에 포함 · 날씨 조회에 `relative_humidity_2m` + 좌표(`geo`).
- ★내 판단 2건(사용자 고지): ① 원본 여성 표에 목적 gathering 줄이 없어 원본 화면은 빈 결과 → casual 줄 대체 ② 원본 문장 표기(「레이어드으로」·rN 나이대 경계 35→40대)는 「그대로」라 무접촉.
- 검증: 순수 35 tests(RED 선확인) + 가드 갱신 · **249 tests** · tsc 0 · build · 뮤테이션 4/4(gathering 대체·남성 판정·문장 경계·탭 잠금 · 대조군 통과 · ⚠M1 역치환이 공백을 남겨 Edit 로 복구) · dev 실화면 = ENFP 선택 → 7탭 전부 · 메이크업 4답 → 「촉촉 광채 데일리 메이크업」 + 19°C·66% · 프로필 저장 실측 · 코디 4장 + 네이버 링크 · 콘솔 0.
- 커밋 `23bedce` · 배포 **`dpl_6Bj31rb2PUJZA8bqqmdLP8uXzA3N`**(READY · 별칭 dpl 일치 · 청크에 메이크업표·그루밍표·습도 질의 실재) · 푸시됨.
- ⚠ Bash heredoc 은 `\n`·`\(` 를 먹는다(오늘 세 번째) — 정규식·이스케이프 든 파일은 Write/Edit 도구로.
- **▶ 눈확인(폰)** = 디렉팅 탭 칩 7 · 유형 전 잠김 · 메이크업 4문항→결과 · 코디 4장(링크) · 문장 · 산책 · 설정 「다시 고르기」. **▶ 미이식** = 마음온도 「오늘의 메뉴」(지시 없음) · 소확행(운세·동행 노트) · 성장기록.

## 10 — (9/22 00시) 디렉팅 칩 → 세로 줄(사용자 *"뭐야? 달라진 게 칩만?"* → 원인 = 유형 전 잠김 + 유형이 기기 저장 안 됨(9/19 이래 화면 메모리뿐) → *"칩을 만들지 말고 마음성향 박스 아래 탭으로 수직배열"*)
- 마음 성향 카드 항상 위 · 아래 줄 6개(아이콘·이름·한 줄 설명·›) · 누르면 그 줄 아래 펼침 · 같은 줄 다시 누르면 접힘 · 한 번에 하나(`dirTab` 재사용 · "persona" = 아무것도 안 펼침) · 유형 전 잠김+안내.
- 가드 갱신(칩 `data-dir-tab`·가로 스크롤 없음 · `data-dir-row` · 토글 식) · 249 tests · tsc 0 · build · 실화면(INFJ → 문장 3 펼침 → 접힘 → 산책 3 · 콘솔 0).
- 커밋 `954e2c2` · 배포 **`dpl_Dor2XD8TE42T5pqPpsnAbEM6DrwY`**(READY · 별칭 dpl 일치 · 청크 세로줄 1 · 칩 0) · 푸시됨.
- **▶ 열린 것** = 유형(personaDone) 기기 저장 — 사용자 답 대기(고치면 디렉팅이 다시 열 때도 열려 있다 · 설정 「유형 바꾸기」 · 전체 파기 포함).

## 11 — (9/22 00시) 디렉팅 줄 잠금 해제 + 유형 기기 저장(사용자 *"왜 탭이 비활성화되어 있어?"* → 제안 둘 다 *"해"*)
- 원인 = ①유형 전 잠금(내 설계) ②유형이 기기 저장 안 됨(9/19 이래 화면 메모리뿐) → 라이브에서 늘 잠긴 채로 보였다.
- ①여섯 줄 항상 열림 · TO do it 만 유형 없으면 안내 한 줄 · 코디 검색어는 유형 없으면 「N대 추천 코디」. ②`persona.ts` `PERSONA_STORE_KEY="wellness-care:persona:v1"` · `parsePersonaDone`(모르는 유형·source·깨진 값 null) · 마운트 로드 · `personaDone` 바뀔 때 저장 · 설정 「마음 성향 유형 → 유형 바꾸기 / 유형 지우기」 · 전체 파기 때 삭제.
- 검증: persona 테스트 +3(RED→GREEN) · 가드 갱신 · **252 tests** · tsc 0 · build · 실화면 = 유형 없이 산책 3 열림·TO do it 안내 → ISFJ 고름 → **새로고침 뒤에도 ISFJ 유지 · TO do it 내용** · 콘솔 0.
- 커밋 `518beb5` · 배포 **`dpl_2KV87MtLx4SFB8SxfsXF4bAFskYd`**(READY · 별칭 dpl 일치 · 청크에 저장 키·「유형 지우기」 실재) · 푸시됨.

## 12 — (9/22) 직접 고른 유형은 피드백 화면 생략 · 음악 줄에 추천 도서(사용자 *"유형을 알아요를 클릭하면 나오는 피드백은 필요없으니 없애줘"* → *"추천도서도 추천음악 탭을 누르면 같이 · 탭 이름 오늘의 추천음악&추천도서 · 배포해"*)
- 격자 클릭 = `personaDone` 저장 + `sheet:null`(디렉팅으로 복귀) · 고르기 화면 ‹ 도 디렉팅으로 · 마음 성향 카드 머리줄 = picked 면 「유형 바꾸기」(→ 격자) · test 면 「결과 다시 보기」(모르던 성향이라 결과 유지).
- 음악 줄 이름 「오늘의 추천음악&추천도서」 · 펼치면 음악 카드 아래 `renderBooksCard`(유형별 두 권 · 성향 결과 2/2 와 같은 목록·네이버 링크 · 유형 없으면 안내).
- 가드 갱신 · 252 tests · tsc 0 · build · 실화면(ENTJ 격자 → 결과 화면 없음 · 카드 「유형 바꾸기」 · 음악 줄 → 도서 2권 링크 · 콘솔 0).
- 커밋 `04615bc` · 배포 **`dpl_9ZVZDviVKUFj4nfcmM6FhiuCcxFf`**(READY · 별칭 dpl 일치 · 청크 줄 이름·격자→디렉팅 실재) · 푸시됨.

## 13 — (9/22) 디렉팅 줄 → 전체 화면 페이지(사용자 *"탭을 누르면 아예 새로운 페이지에서 나오게"*)
- 줄 클릭 = `patch({ dirTab, sheet: "directing" })` → `renderDirectingSheet`(다른 시트와 같은 껍데기 · ‹ 로 복귀 · 제목=줄 이름 · 부제=한 줄 설명) · 본문은 `renderDirectingBody` 한 곳 · 줄 목록은 `directingRows` 헬퍼 하나(목록·페이지 공용) · 목록 화면엔 제자리 펼침 없음.
- 가드 갱신 · **19 파일 · 252 tests** · tsc 0 · build · 실화면(코디 줄 → 페이지 · 카드 4 · ‹ → 목록 · 콘솔 0).
- ⚠ 박제: heredoc 이 정규식 `[^\n]` 을 실제 줄바꿈으로 바꿔 가드 파일이 **파싱 실패로 통째로 빠졌는데** `Tests … passed` 만 보고 넘어갈 뻔했다(252→239). 🚫 `Tests` 줄만 보지 말고 **`Test Files N passed (N)`** 까지 볼 것.
- 커밋 `b4fd566` · 배포 **`dpl_kvtHN4nm2Fwnep8Z66ybF8E8S7XD`**(READY · 별칭 dpl 일치 · 청크 `data-directing-sheet` 실재) · 푸시됨.

## 14 — (9/22) 여섯 페이지 전부 성별·나이대 먼저 + 「내 프로필」 카드(사용자 *"오늘의 메이크업은 연령 선택은?"* → 실측 메이크업 원본은 나이 미사용·코디만 → *"전부 성별 나이는 물어야 하는 거 아냐?"*)
- `renderDirectingBody` 첫 줄 = 프로필 없으면 `renderProfilePick()`(문구 「디렉팅은 성별과 나이대에 맞춰 드려요」) · 목록 마음 성향 카드 아래 `data-profile-card`(없으면 그 자리에서 고르기 · 있으면 「남성 · 40대 — 바꾸기 ›」).
- 가드 갱신 · 19 파일 · 252 tests · tsc 0 · build · 실화면(프로필 지움 → 산책 줄 → 프로필 화면 → 저장 → 코스 3 → 목록 카드 값 · 콘솔 0).
- 커밋 `86e500b` · 배포 **`dpl_3oViDvXjrcTUfyAP13Gimahee1DX`**(READY · 별칭 dpl 일치 · 청크 실재) · 푸시됨.

## 15 — (9/22 저녁) 홈 「월간 기록 보기」 밑 「데일리케어 바로가기」·「디렉팅 바로가기」 + 하단 탭 「케어」→「데일리 케어」(사용자 지시 그대로)
- `renderWeekFlow` footer = 세로 두 줄(월간 보라 채움 / 바로가기 둘 반반 · 옅은 보라 보조 단추) · 바로가기 = `patch({ tab: "care" })` / `patch({ tab: "healing" })` · `renderTabs` 라벨만 변경(탭 id·아이콘 무변경).
- 가드 갱신(월간=sheet month · 바로가기 대상 둘 · 라벨 「데일리 케어」) · **19 파일 · 252 tests** · tsc 0 · 뮤테이션 2/2(라벨 되돌리기 · healing→care) · 실화면(홈 세 단추 → 각 탭 이동 · 하단 탭 넷).
- ⚠ 박제: 뮤테이션 되돌리기에 `git checkout --` 을 써서 미커밋 편집이 한 번 날아갔다(재적용 · 손실 없음). 🚫 문자열 역치환으로만 되돌릴 것(이 레포 규칙 재확인).
- 커밋 `d0329e3` · 배포 **`dpl_4J5HPk8HsEPwZPPozo8bsUKqgvbb`**(READY · 별칭 `wellness-care-zeta.vercel.app` dpl 일치 · 라이브 청크에 「데일리케어 바로가기」·「데일리 케어」 실재) · 푸시됨.
- **15-보강** 바로가기 두 단추 파스텔(사용자 *"너무 튀지 않은 파스텔로"*) = 데일리케어 민트 `#d9f0ea`/`#2b7a6f` · 디렉팅 라벤더 `#e6e0f8`/`#5f5397` · 약한 그림자. 커밋 `1ea9c5f` · 배포 **`dpl_E3NhpDfisWC3ccFC6bAZXjk1a2MT`**(READY · 별칭 dpl 일치 · 청크에 두 색 실재) · 푸시됨.

## 16 — (9/22 밤) 「마음과 대화」 → 「오늘, 어떤 하루였나요?」 전면 개명 + 대화 화면 탭 줄 삭제(사용자 *"마음과대화를 오늘,어떤 하루였나요?로 · 케릭터를 설정하고 들어가면 마음과 대화 탭과 오늘의 마음카드 탭을 삭제 · 모두 수정"*)
- **탭 삭제** = 캐릭터를 고르고 들어간 **대화 화면**(`chatRef` 블록) 머리줄의 탭 줄 두 개. 돌아가는 길은 머리줄 ‹(마음 건강). 🚫 여기에 탭을 되살리지 말 것(가드).
- **개명 9자리** = `CHARACTER_DISPLAY_NAME`(대화창 표시 이름) · 마음 건강 시트 섹션 제목 · 마음카드 화면 탭 · 월간 통계 칸 · 월간 제안 문장 · `condition.ts` 근거 2곳·해석 문장 1곳 · `risk.ts` 시스템 프롬프트 · 주석/테스트.
- ★ 문장 속 넷은 **낫표로 감쌌다**(「오늘, 어떤 하루였나요?」에 잠깐 들러 보세요 / …에서 요즘 이야기 꺼내보기 / 「…」 N번) — 그대로 치환하면 "오늘은 오늘, 어떤 하루였나요?에" 처럼 문장이 깨진다.
- ★ 월간 통계 칸은 1/3 폭 `white-space:nowrap` 이라 긴 이름이 넘친다 → `word-break:keep-all`+두 줄 허용. **DOM 실측 width 79 = scrollWidth 79**(넘침 0 · 그 칸만 두 줄).
- 가드 2건 신설(화면 코드에 「마음과 대화」 0건 · 대화 화면 머리줄에 「오늘의 마음카드」·`sheet:"picture"` 없음 · `sheet:"mind"`는 남음) · **19 파일 · 254 tests** · tsc 0 · 뮤테이션 1/1(탭 되살리면 실패) · 실화면(케어 칩 → 상대 고르기 제목 → 캐릭터 → 대화 화면 탭 없음 · 월간 칸).
- 커밋 `8be4566` · 배포 **`dpl_DYam2yK55Q6kM2aVwQfuu8sTpYND`**(READY · 별칭 dpl 일치 · 라이브 청크 새 이름 2건 · **옛 이름 0건**) · 푸시됨.

## 17 — (9/22 밤) 「오늘, 어떤 하루였나요?」 음성 입력 — 말로 묻고 답은 글로(사용자 *"묻는걸 말로하고 답은 글로나오고"* → *"대화하는것 처럼 자연스럽게"* → **(나) 이어 말하기** 확정)
- 마이크 한 번 → 듣기 → 말한 글자가 입력칸에 차오름 → 말 끝나면 **자동 전송** → 답이 글로 온 뒤 **스스로 다시 듣기**. 한 번 더 누르면 멈춤.
- 판정은 `speech.ts` **순수 모듈 한 곳**(`speechSupport`·`recognitionCtor`·`shouldSend`·`cleanTranscript`·`shouldResume`·`micHint`·`SILENT_STOP_MS`) — 이 레포엔 jsdom 이 없어 화면 안에 두면 영원히 미검증.
- 안전장치 = 10초 무음이면 자동 종료 · 빈 소리 미전송 · 「듣고 있어요」 상시 표시 · 권한 거부 안내 한 줄 · **위험 응답이 뜬 자리에서는 재개하지 않는다**(`shouldResume` · 상담 안내를 읽는 자리).
- 🚫 **답을 소리로 읽어 주지 말 것** — 마이크가 제 소리를 되받아 되묻는다(가드 `speechSynthesis` 0건). 🚫 `micOk` 없이 마이크를 그리지 말 것.
- ★ `send()` 가 인자를 받게 바꿨다(`send(text?)`) — 그 바람에 `onClick={send}` 가 MouseEvent 를 본문으로 넘기던 것을 tsc 가 잡았다(`onClick={() => send()}`).
- 신규 순수 12 + 가드 4 · **20 파일 · 270 tests** · tsc 0 · 뮤테이션 1/1 · **실화면 전 과정**(가짜 인식기로 켜기→중간 글자→전송→답→**재시작 2회**→끄기→권한 거부 안내).
- ⚠ **아이폰 사파리 지원은 미확인**(이 창은 크로미움) — 폰 눈확인 필요. 안 보이면 그 브라우저가 지원 안 하는 것이고 키보드 받아쓰기로 쓰면 된다.
- 커밋 `f87b820` · 배포 `dpl_6vnWFP28zbmvzzHg7TmhmniuNUnc`.

## 18 — (9/22 밤) 케어 탭 마음 박스 — 나란한 칩 둘 → 세로 줄 둘(사용자 *"오늘, 어떤 하루였나요? 와 오늘의 마음카드의 구분이 겹쳐보인다"*)
- ★ **실측상 겹치지 않았다** — 375px 에서 39~183 / 191~337(8px 간격 · `scrollWidth==clientWidth` 넘침 0), 320px 에서도 안 겹침. 겹쳐 **보인** 이유는 8px 간격에 같은 색·굵기·크기라 한 덩어리로 읽힌 것 + 바로 위 부제가 같은 두 가지를 또 말한 것.
- → 디렉팅 탭 줄과 **같은 모양의 세로 두 줄**(아이콘 · 이름 · 한 줄 설명 · ›). 각 전폭 297px · 10px 간격(실측). 🚫 가로 두 칸으로 되돌리지 말 것(가드 `data-mind-row`).
- 20 파일 · 270 tests · tsc 0 · 실화면 확인.
- 커밋 `5e28957` · 배포 **`dpl_3apHKbUJDSypvx47h5JbMipMGFy4`**(READY · 별칭 dpl 일치 · 라이브 청크에 `data-mind-row`·「음성으로 말하기」 실재) · 푸시됨.

## 19 — (9/22 밤) 폰에서 마음 건강 시트 두 섹션이 겹치던 것(사용자 사진 — 웹은 멀쩡, 폰만 겹침)
- ★ **원인 = `flex:1`(= shrink 1)** — 세로가 모자라면 박스가 **내용보다 작게 줄어드는데** 넘친 내용을 자르지도 스크롤하지도 않아 구분선과 아래 섹션 위로 흘러나왔다. 웹은 화면이 길어 안 줄었고 폰은 주소창까지 있어 줄었다.
- **실측(375×600 재현)** 고치기 전 `clientHeight 247 < scrollHeight 294`(47px 유출 · 안내 줄 top 376 > 박스 bottom 348) → 고친 뒤 **381 = 381** · 섹션1 100~482 · 구분선 482~484 · **섹션2 484~719** (겹침 0 · 바깥 컨테이너만 스크롤 619>500).
- **고침 = 두 곳 `flex:1` → `flex:1 0 auto`**(늘어나되 줄어들지 않는다 · 기준 높이 = 내용). 「반씩 꽉 채운다」 원래 지시는 그대로 — 긴 화면에서는 여전히 늘어난다. 🚫 `flex:1` 로 되돌리지 말 것(가드 = 그 문자열 2개 존재 + 옛 문자열 0개).
- 20 파일 · **272 tests** · tsc 0.
- 커밋 `d7e6149` · 배포 **`dpl_6KWduVYkjW35dM2Wh25zrUqrxC7v`**(READY · 별칭 dpl 일치 · 라이브 청크에 `flex:1 0 auto; min-height:230px` 2건) · 푸시됨.

## 20 — (9/22 밤) 말할 때마다 마이크 「차단·허용」 창이 뜨던 것(사용자 보고)
- ★ **원인 = `continuous:false`** — 한 마디가 끝날 때마다 인식을 **새로 시작**했고(턴마다 `new Ctor()` 까지), 브라우저는 그것을 **매번 새 마이크 요청**으로 본다.
- **고침** = `continuous:true` + **인식기 하나를 끝까지 재사용**. 답을 쓰는 동안에도 끊지 않고 `waitingRef` 로 **보내기만** 멈춘다(끊으면 다음 말에 또 뜬다). 브라우저가 조용해서 스스로 끝내면 **같은 인식기**를 다시 `start`.
- **실측(가짜 인식기로 두 턴 왕복)** = recognizer 생성 **1회 · start 1회**(그전 구조면 턴마다 새로 만들었다) · 두 발화 모두 전송 · 답 쓰는 중 발화는 **안 새어 나감** · 끄기 정상.
- ⚠ **앱 안 브라우저(카톡 등)는 허용을 기억하지 않아 페이지를 열 때마다 한 번은 남는다** — 코드로 못 없앤다. 크롬·사파리로 열거나 홈 화면에 추가하면 기억된다.
- 🚫 `continuous` 를 false 로 되돌리거나 턴마다 새 인식기를 만들지 말 것(가드 3건 · 뮤테이션 1/1).
- 20 파일 · **276 tests** · tsc 0.
- 커밋 `34ad5e4` · 배포 **`dpl_34M5c8rL5jSn299jxmD4ZcDEoTJc`**(READY · 별칭 dpl 일치 · 라이브 청크에 continuous=true 실재) · 푸시됨.
