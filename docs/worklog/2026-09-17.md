# 2026-09-17 웰니스 케어 작업일지

## 1 — 홈 재구성 8건 (사용자 지시 두 차례)
지시 원문(요지): 인사말을 현재 시각에 맞게 · 「오늘의 기록」 맨 위 · 종합 컨디션 박스의 「좋음」·최근 흐름·신체/마음 칩 삭제 · 주간 기록 보기 삭제(주간 흐름 막대·범례는 기록 화면 앞에) · 「오늘 몸 상태 3가지」는 홈이 아니라 앞의 문진으로, 체크되면 딜레이 · 「AI 오늘의 제안」에서 AI 삭제 · 제안을 접속 시간에 맞게 · 「지금 바로 시작하기」 3개 선택.

### 한 것
| # | 무엇 | 어디 |
|---|---|---|
| 1 | 인사말 시간대별(5~11 아침 / 11~14 점심 / 14~18 오후 / 18~22 저녁 / 밤) | `greeting.ts` 신설(순수·테스트 4) |
| 2·3 | 홈 순서 = 헤더 → **오늘의 기록** → **「기록 보기 ›」 버튼 하나** → 날씨 → 오늘의 제안. 종합 컨디션 박스(큰 단계 글자·최근 흐름·신체/마음 칩·주간/월간 두 칩)는 통째 삭제 | `renderHome` |
| 4 | 「주간 기록 보기」 시트(`renderCondition`) 삭제. **이번 주 흐름 막대 + 범례**만 `renderWeekFlow` 로 떼어 **월간 기록 화면 맨 위**(my 탭·홈 시트 공용) | `renderRecords` |
| 5 | 「오늘 몸 상태」 3문항을 홈에서 빼 **안전 확인 문진 안**(후속 묶음 아래·판정문 위)으로. 하나라도 예 → 판정문 「오늘은 쉬어 가요」 + 홈 제안 카드가 **프로그램·버튼 대신 쉬기 안내**. 날짜 초기화 없음 — 해제는 설정 「안전 확인 다시 답하기」(안내문이 그 길을 말한다). `safetyComplete` 가 미루기 3개까지 요구 | `safety-screen.ts`·온보딩 |
| 6 | 배지 「AI 오늘의 제안」→「오늘의 제안」 | |
| 7 | `DEMO_PINNED_SLOT` 2 → **null** — 아침·점심·퇴근 전 항목이 접속 시각을 따른다(09-12 시연 고정 해제) | `suggestion.ts` |
| 8 | `suggestionChoices` 신설 — 0·1단계는 직군 세 항목 전부(시간대 맞는 것 첫째·보라 채움, 나머지 흰 버튼) · 2단계는 숨 고르기 하나. 고른 항목은 `pick` 상태로 시트에 전달(라이브러리·「1분 기지개부터」는 `pick:null`) | `suggestion.ts`·홈 |

### 해석해서 정한 것(틀리면 되돌린다)
- 「주간 기록 보기 탭 삭제 … 대신 주간 흐름 바와 범례」 = 주간 화면을 없애고 그 막대만 월간 화면 맨 위에. 「하루씩 보기」·「어떻게 정했나요」 각주는 주간 화면과 함께 사라졌다.
- 「체크되면 딜레이」 = **제안만** 쉬기로(09-16 「잠글 필요 없다」는 라이브러리·데일리케어에 남김).
- 「월간 기록 보기 탭」 = 이미 my 탭이 월간 기록이라 새로 만들지 않았다(홈 「기록 보기」 버튼이 같은 화면을 시트로).

### 검증
tsc 0 · **137/137**(130 → +7) · build · ⚠**뮤테이션 2/2**(대조군 137 → ①선택지 첫째를 시간대와 무관하게 2실패 ②미루기 미답도 완료 1실패 → 원복 137) · greeting 은 상수 뮤테이션으로 RED 확인(2실패) · ★**실화면(dev · 00:30)**: 안전 확인에 「오늘 몸 상태」 3줄(9+3=12) · 미루기 미답이면 판정문 잠김 → 답하면 열림 · 홈 순서 「오늘의 기록@46 → 기록 보기@124 → 오늘의 제안@149 → 지금 바로 시작하기@248」 · 인사말 「이제 쉬어도 되는 시간이에요」 · 「AI」·「종합 컨디션」·「주간 기록 보기」·홈의 「오늘 몸 상태 확인」 **0건** · 선택지 3개(수업 전 / 4교시 후 / 퇴근 전) · 둘째를 누르면 시트 제목이 그 항목(타이머) · 셋째는 영상 · 「기록 보기 ›」 → 시트에 「이번 주 흐름」 + 범례(휴식 필요~매우 좋음) · 설정에서 감기 「예」 → 판정문 「오늘은 쉬어 가요」 → 홈 제안 카드에 버튼 0·안내 1·데일리케어 그대로.

### ⚠ 남은 것
- 밤(22~5시)엔 `slotForHour` 가 「아침」 항목을 첫째로 둔다(새벽 0:30 에 「수업 전 …」이 첫째였다) — 인사말은 「쉬어도 되는 시간」이라 어긋난다. 밤 시간대 항목을 따로 둘지 판단 대기.
- 아침·점심 항목엔 가이드 영상이 없어(퇴근 전만) 그 시간대 첫째 선택지는 타이머 화면.
- 배포 **`dpl_BNKiUG3NQ146nzhxhmoWPuHw4VKk`**(READY · 별칭 `wellness-care-zeta.vercel.app` = `mo22ub858` 일치) · 라이브 청크 「AI 오늘의 제안」·「오늘도 한 걸음 왔네요」·「주간 기록 보기」 **0건**, 「하나를 골라 주세요」·「이제 쉬어도 되는 시간이에요」·「기록 보기 ›」·「오늘은 쉬어 가요」 각 1건.

## 2 — 정정: 주간 흐름 막대·범례는 **홈 박스**에, 버튼은 「월간 기록 보기」 (사용자: "기록보기 탭이 아니고 월간기록보기 탭인데 주간흐름 슬라이드바와 범례는 보이게")
- ⚠★**내가 잘못 읽었다(박제)** — 1건차에서 「주간 기록 보기 삭제 … 대신 주간 흐름 바와 범례」를 「막대를 월간 화면 맨 위로 옮기라」로 읽고 홈엔 「기록 보기」 버튼만 남겼다. 뜻은 **홈의 그 자리에 막대·범례가 보이고, 버튼이 「월간 기록 보기」**였다. 🚫 홈 박스에서 막대를 다시 치우지 말 것.
- `renderWeekFlow(footer)` 로 홈 박스(막대 + 범례 + 「월간 기록 보기 ›」) · 월간 화면 맨 위에 넣었던 막대는 뺌(중복 방지). 월간 시트 부제 「하루씩 보기는 my 탭에서」(사라진 화면 지칭) → 「my 탭과 같은 화면」.
- 검증: tsc 0 · 137/137 · build · ★실화면: 홈 순서 「오늘의 기록@46 → 이번 주 흐름@124 → 월간 기록 보기@188 → 오늘의 제안@216」 · 범례(휴식 필요~매우 좋음) · 「월간 기록 보기 ›」 → 월간 시트(막대 중복 0).
- 배포 = 미실시(승인 대기).
